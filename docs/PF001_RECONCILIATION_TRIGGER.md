# PF-001 reconciliation trigger

This temporary marker starts the owner-authorized, externally attested transition from `validated_on_branch` to `merge_pending` after the corrected immutable attestor became active on `main`.

It must be removed before the reconciliation PR is merged.
