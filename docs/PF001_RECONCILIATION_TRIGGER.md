# PF-001 reconciliation trigger

This temporary marker initiates the externally attested transition from `validated_on_branch` to `merge_pending` using the corrected controller that preserves the branch-validation receipt.

It must be removed before merge.
